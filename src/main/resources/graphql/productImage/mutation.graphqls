extend type Mutation {
    addProductImage(
        productId: ID!
        imageToken: String!
    ): ProductImageResponse

    addProductVariantImage(
        variantId: ID!
        imageToken: String!
    ): ProductImageResponse
}
