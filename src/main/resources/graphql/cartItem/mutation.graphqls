extend type Mutation {
    addToCart(input: CartItemInput!): CartItemResponse!

    updateCartItemQuantity(
        cartItemId: ID!
        quantity: Int!
    ): CartItemResponse!

    removeFromCart(
        cartItemId: ID!
    ): Boolean!
}